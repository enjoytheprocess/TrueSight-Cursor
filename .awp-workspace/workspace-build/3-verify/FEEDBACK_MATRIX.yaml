# Human Feedback Matrix — structured test observations from human review.
# Edit this file directly or ask your agent to update it.
# Workflow rules: docs/core/workflow-reference.md
#
# type: observation | issue | pass | regression
# severity: low | medium | high
# status: open | resolved_in_loop | resolved
#
# diagnosis: optional free-text field — human's interpretation of the root cause.
#   The agent reads this when creating a GAPS_AND_DEVIATIONS entry.
#   Humans do not need to classify gap vs. deviation; the agent handles that.

schema_version: 2

entries:
  - id: FM-008
    task_id: BUILD-SHP-004
    feature_id: FEAT-SHP-001
    type: observation
    severity: low
    status: resolved_in_loop
    summary: "Tab headline (top h1) does not use full shell width on desktop"
    detail: >
      After the two-column shell widens (>840px), the header eyebrow + tab headline stay in a
      narrow column instead of expanding with the window. Likely `h1 { max-width: 680px }` while
      `.main-column` is now `max-width: 1180px`.
    diagnosis: "Remove or raise the h1 max-width for wide viewports; align topbar with shell-workspace width"
    recorded_at: "2026-05-26"
    recorded_by: human_reviewer
    resolution_note: >
      styles.css — full-width centered topbar headline; container-query font sizing and
      text-wrap balance/pretty to avoid orphan words at max shell width.
