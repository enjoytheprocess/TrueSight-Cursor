# Snippets API

Workspace initialized in direct-use mode.

- Repo mode: `single-component`
- Primary component: `api`

A small REST API for storing and retrieving code snippets.

## Human quick path
- Read `docs/core/workflow-summary.md`.
- Use `1-design/PROJECT_BRIEF.md`, `2-build/WORK_QUEUE.md`, and `3-verify/TRACEABILITY_MATRIX.md` as the live workflow state.
- Use the component docs under `components/api/` for component-specific setup.

## Common commands
- `make docs-check`
- `make install-hooks`
